{
  "name": "dataset-reviewer",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Public, in-browser semantic search over newly-proposed ML/NLP datasets and benchmarks.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@huggingface/transformers": "^3.3.0"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
